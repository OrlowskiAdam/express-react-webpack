import * as React from 'react';

const App = (props: AppProps) => {
    return (
        <>
            <h1>
                Hello world!
            </h1>
            <p>Express React Webpack project!</p>
        </>
    );
};

interface AppProps {
}

export default App;
